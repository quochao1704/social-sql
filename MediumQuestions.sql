
-- =========================
-- Level 2: Medium Questions
-- Focus: JOINs, GROUP BY, HAVING, Subqueries, Date functions
-- =========================

-- 11. User post count
-- Count how many posts each user has created.
-- Show username and post count.

-- 12. Active users with posts
-- Find all active users who have created at least one post.

-- 13. Posts without tags
-- Find all posts that don't have any associated tags.

-- 14. Most popular action type
-- Find which action_type occurs most frequently in the actions table.

-- 15. Users registered this year
-- Find all users who registered in the current year (2025).

-- 16. Posts per month
-- Count how many posts were created in each month of 2024.

-- 17. Users with no posts
-- Find all users who haven't created any posts yet.

-- 18. Average posts per user
-- Calculate the average number of posts per user
-- (only for users who have posts).

-- 19. Posts with multiple tags
-- Find posts that have more than 2 tags associated with them.

-- 20. User engagement
-- For each user, count their total number of actions
-- and posts combined.

-- 21. Recent active users
-- Find users who have performed any action in the last 30 days.

-- 22. Tag popularity
-- Find the top 5 most used tags
-- (tags applied to the most posts).

-- 23. Inactive users cleanup
-- Find users who are inactive AND have never created a post.

-- 24. Post deletion rate
-- Calculate what percentage of posts are marked as deleted.

-- 25. User activity timeline
-- For each user, find their first post date
-- and their most recent action date.

